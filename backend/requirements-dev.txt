# Development and CI tooling. Kept separate so the production image never
# installs a test framework or a linter.
-r requirements.txt

pytest==8.3.4
pytest-cov==6.0.0
httpx==0.28.1
ruff==0.8.4
mypy==1.14.0
types-boto3==1.0.2
